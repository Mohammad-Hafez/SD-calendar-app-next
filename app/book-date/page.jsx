import BookDate from '../../components/BookDate/BookDate'
export const metadata = {
  title: 'Book Date',
  description: 'book a future date',
}

export default function BookDatePage() {
  return <BookDate />
}